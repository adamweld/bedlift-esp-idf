# UI icon manifest. One icon per line:
#
#   <name> <size> [source.png]
#
# <name>    what the UI code passes to ui_icon_draw()
# <size>    target size in px (longest side; aspect is kept)
# source    PNG in this folder; defaults to <name>.png. Name an alias to use
#           one PNG at two sizes.
#
# PNGs: shape in the alpha channel (Tabler / Material downloads are black on
# transparent), or opaque dark-on-light. Any resolution; downsampled
# anti-aliased. Rotation is applied at draw time, not here.
#
# Regenerated automatically by the build (scripts/gen_icons.py).

# mode panel (left column)
arrows-up-down          64
view-360-arrow          64
rotate-360              64
stretching              64
box-align-bottom-right  64

# status bar
cycle                   20
